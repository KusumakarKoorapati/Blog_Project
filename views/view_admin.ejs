            
            
            <%-include('header') %>

            <!-- Content wrapper -->
            <div class="content-wrapper">
              <!-- Content -->
  
              <div class="container-xxl flex-grow-1 container-p-y">
                <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Tables /</span> Basic Tables</h4>
                <!-- Bootstrap Dark Table -->
                <div class="card">
                  <h5 class="card-header">Table Dark</h5>
                  <div class="table-responsive text-nowrap">
                    <table class="table table-dark">
                      <thead>
                        <tr>
                          <th>Name</th>
                          <th>Email</th>
                          <th>Gender</th>
                          <th>City</th>
                          <th>Hobby</th>
                          <th>Image</th>
                          <th>Action</th>
                         
                        </tr>
                      </thead>
                      <tbody class="table-border-bottom-0">
                        <% for(var ad of Admindata){ %>
                        <tr>
                          <td><i class="fab fa-angular fa-lg text-danger me-3"></i><strong><%=ad.name %></strong></td>
                          <td><%=ad.email %></td>
                          <td><%=ad.gender %></td>
                          <td><%=ad.city %></td>
                          <td><%=ad.hobby %></td>
                          <td><img src="<%=ad.image %>" height="50" width="50"></td>
                          <td>
                            <a href="/DeleteRecord/<%=ad.id%>" class="btn btn-danger btn-sm">Delete</a>
                            <a href="/UpdateRecord/<%=ad.id%>" class="btn btn-primary btn-sm">Update</a>
                          </td>
                          
                        </tr>
                        <% } %>
                      </tbody>
                    </table>
                  </div>
                </div>
                <!--/ Bootstrap Dark Table -->
              </div>
              <%-include('footer') %>