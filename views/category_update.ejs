<%-include('header') %>
        

              <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Forms /</span> Basic Inputs</h4>
                <!-- Form controls -->
              <form method="post" action="/category/EditRecord">
                <input type="hidden" name="editId" value="<%=data.id %>">
                <div class="col-md-6">
                  <div class="card mb-4">
                    <h5 class="card-header">Form Controls</h5>
                    <div class="card-body">
                        <div class="mb-3">
                            <label for="defaultFormControlInput" class="form-label">Title</label>
                            <input type="text" class="form-control" name="category" value="<%= data.category %>" id="defaultFormControlInput" placeholder="John Doe"aria-describedby="defaultFormControlHelp"/>
                        </div>
                      <div class="mt-3">
                        <input type="submit" value="Update Offer" name="submit" class="btn btn-danger">
                      </div>
                      
                    </div>
                  </div>
                </div>
              </form>

           <%-include('footer') %>